import React from 'react'

const User = (): JSX.Element => {
  return (
    <div>user-personal.tsx ? / single-user/ userprofile / profile (Personal user information, personal user page) opportunity to upload photo (bezKoder - file upload with axios and progress/ lama dev blog-app how to uload a file to server)</div>
  )
}

export default User
