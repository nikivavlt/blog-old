import React from 'react'

const Dashboard = (): JSX.Element => {
  return (
    <div>Dashboard (or List of users, protected route and access required from backend)</div>
  )
}

export default Dashboard
